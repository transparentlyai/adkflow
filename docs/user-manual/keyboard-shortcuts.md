# Keyboard Shortcuts

All keyboard shortcuts in ADKFlow.

## General

| Shortcut | Action |
|----------|--------|
| `Ctrl+O` / `Cmd+O` | Open project switcher |
| `Ctrl+S` / `Cmd+S` | Save current tab |
| `Ctrl+Z` / `Cmd+Z` | Undo |
| `Ctrl+Shift+Z` / `Cmd+Shift+Z` | Redo |
| `Escape` | Cancel current action / Deselect all |

## Selection

| Shortcut | Action |
|----------|--------|
| `Ctrl+A` / `Cmd+A` | Select all nodes |
| `Shift+Click` | Add/remove from selection |
| `Click+Drag` | Box selection |
| `Escape` | Deselect all |

## Editing

| Shortcut | Action |
|----------|--------|
| `Ctrl+C` / `Cmd+C` | Copy selected nodes |
| `Ctrl+X` / `Cmd+X` | Cut selected nodes |
| `Ctrl+V` / `Cmd+V` | Paste nodes |
| `Delete` / `Backspace` | Delete selected nodes |
| `Ctrl+D` / `Cmd+D` | Duplicate selected nodes |

## Navigation

| Shortcut | Action |
|----------|--------|
| `Scroll` | Pan canvas (when not over node) |
| `Ctrl+Scroll` / `Cmd+Scroll` | Zoom in/out |
| `Double-click (canvas)` | Fit view |
| `Arrow keys` | Move selected nodes |

## Canvas

| Shortcut | Action |
|----------|--------|
| `Right-click` | Open context menu |
| `Space+Drag` | Pan canvas |
| `+` / `=` | Zoom in |
| `-` | Zoom out |
| `0` | Reset zoom to 100% |

## Nodes

| Shortcut | Action |
|----------|--------|
| `Enter` | Expand/collapse selected node |
| `Tab` | Cycle through nodes |
| `Shift+Tab` | Cycle through nodes (reverse) |

## Workflow

| Shortcut | Action |
|----------|--------|
| `F5` | Run workflow |
| `Ctrl+Enter` / `Cmd+Enter` | Run workflow |
| `Shift+Escape` | Cancel running workflow |

## Platform Differences

### macOS

Replace `Ctrl` with `Cmd` (⌘) for all shortcuts.

### Windows/Linux

Use `Ctrl` as shown.

## Customization

Currently, keyboard shortcuts cannot be customized. This may be added in a future version.

## Tips

### Efficiency

- Use `Ctrl+C` / `Ctrl+V` liberally
- `Ctrl+Z` is your friend
- Box selection is faster than Shift+Click

### Navigation

- Double-click to fit view when lost
- Use minimap for large workflows
- Scroll to pan, Ctrl+Scroll to zoom

## See Also

- [Interface Overview](./interface-overview.md) - UI reference
- [Canvas](./canvas.md) - Canvas navigation
- [Nodes](./nodes.md) - Working with nodes
