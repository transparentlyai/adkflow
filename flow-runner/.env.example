# ============================================================================
# Authentication Configuration
# ============================================================================

# Option 1: Google AI Studio (API Key)
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_api_key_here

# Option 2: Vertex AI (Application Default Credentials)
# Set GOOGLE_API_KEY=vertex to use Vertex AI with ADC
# Requires: gcloud auth application-default login
# GOOGLE_API_KEY=vertex

# ============================================================================
# Vertex AI Configuration (only needed if using GOOGLE_API_KEY=vertex)
# ============================================================================

# Required: Google Cloud Project ID
# GOOGLE_CLOUD_PROJECT=your-project-id

# Optional: Google Cloud Region (default: us-central1)
# GOOGLE_CLOUD_REGION=us-central1

# Optional: Default model to use if not specified in workflow
# DEFAULT_MODEL=gemini-2.0-flash-exp

# Optional: Default temperature for model generation
# DEFAULT_TEMPERATURE=0.7

# Optional: Enable verbose logging
# VERBOSE=false

# Optional: Maximum LLM calls per workflow execution
# MAX_LLM_CALLS=50

# Optional: Request timeout in seconds
# REQUEST_TIMEOUT=120
